node_modules/
dist/
playwright-report/
test-results/
.DS_Store
.env

# Оригиналы картинок (исходники до сжатия в WebP) — хранятся локально, в архив/репо не нужны
scripts/images-raw/

# Транзиентные артефакты пайплайна вопросов (I/O воркфлоу, шарды по категориям)
scripts/gen-out/
scripts/polish-in/
scripts/polish-out/
scripts/factcheck-in/
scripts/factcheck-out/
scripts/rerepair-in/
scripts/rerepair-out/
scripts/rescue-in/
scripts/rescue-out/
scripts/analysis-in/
scripts/analysis-out/
scripts/rejudge-in/
scripts/rejudge-out.json
scripts/angleflip-in/
scripts/angleflip-ids.json

# Бэкапы данных
public/questions.backup-*.json
public/*.backup-*.json
scripts/questions.backup-*.json
scripts/questions.reviewed-backup.json
data/*.backup-*.json

# Пул ревью и журнал (data/review-pool.json, data/review-log.jsonl) — НЕ игнорируем:
# это рабочая база ревью и обучающий корпус, они должны быть в репозитории.

# Локальная конфигурация инструментов (но settings.json — командный, в репозитории)
.claude/*
!.claude/settings.json
