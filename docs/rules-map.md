# Карта связности правил (что обновлять вместе)

Проблема: одно правило живёт прозой в нескольких местах (промпты генератора/судьи,
свод, доки, тесты). Меняешь в одном — забываешь в другом → дрейф. Эта карта делает
протяжку механической: меняешь правило → проходишь ВСЕ зависимые точки по списку.

**Дисциплина:** любое изменение правил генерации/судейства/ревью или промптов —
сначала ПРЕДЛОЖЕНИЕ на аппрув, потом правка по ВСЕМ точкам ниже, потом `npm test`.
Не коммитить молча. (См. память `feedback_pipeline_changes`.)

---

## Лимит длины ответа

- **Каноничный код (источник истины):** `src/content-rules.js` — `MAX_ANSWER_WORDS`,
  `MAX_ANSWER_CHARS`, `MAX_WORD_CHARS`, `FUNCTION_WORDS`, `contentWords`, `validateAnswerText`.
- Зависимые точки (обновлять вместе):
  - `scripts/fill-questions.workflow.js` — genPrompt (лимит при генерации) + код-гейт длины при слиянии.
  - `scripts/merge-gen.mjs` — код-гейт: длинные генерации не пускать в пул чистым keep.
  - `scripts/merge-polish.mjs` — гейт длины провален → применить текст полиша ЦЕЛИКОМ
    (вопрос+ответы когерентны) + статус rework на укоротку. НЕ откат к оригиналу (рассинхрон).
  - `scripts/prep-polish-revise.mjs` — для length-rework узкая директива «укороти ответы, вопрос не трогай».
  - `scripts/polish.workflow.js` — правило 9 (доводка укорачивает).
  - `scripts/lib/review-store.mjs` — `gatesOk` (промоушн в прод).
  - `src/main.js` — `validateQuestions` (мягкое предупреждение в игре).
  - `src/admin/admin.js` — предупреждение в карточке ревью.
  - `tests/content-rules.test.js`, `tests/questions-answers.test.js` — гейты.
  - `docs/category-risks.md` (A.5 + свод), `docs/GDD_LITE.md` (§4).
- **Принцип:** длина детерминирована → проверяется КОДОМ (`validateAnswerText`), НЕ LLM.

## Запрещённые темы ЯИ (3.4)

- **Каноничный код:** `src/content-rules.js` — `PROHIBITED_TOPIC_RE`, `validateNoProhibited`.
- Зависимые: `src/main.js` (validateQuestions), `fill-questions`/`polish` (флаг prohibitedYG,
  правило), `docs/category-risks.md` (A.0). Часть на LLM-судье (политика/религия — регэкспом не отсечь).

## Гейты уровня ВОПРОСА (около-спам, вложенные диапазоны, перечисление через «и»)

- **Каноничный код:** `src/content-rules.js` — `validateOptionsHomogeneous` (около-спам ≥2, жёсткий),
  `validateHedgeTell` (хедж ровно в 1 варианте → телеграф верного, мягкое), `validateNumericRanges`
  (вложенные «более N»/«менее N», жёсткий), `validateNumericTell` (верный — единственное не-круглое
  число, мягкое; берёт `correctAnswerIndex`), `validateEnumeration` (запятая→«и», мягкое),
  `validateExamShape` (вопрос «Почему/Зачем» + все 4 ответа >2 слов = эссе-экзамен, мягкое; берёт
  `question`+`answers`). Проверяют 4 варианта ВМЕСТЕ (часть — с вопросом), а не один ответ.
  ⚠ Границу слова `\b` с кириллицей НЕ использовать (`\w` её не включает) — сверять по набору слов.
- Семантические правила (кодом не отловить, только промпт+свод): редкий термин в вопросе
  поясняй; **малоизвестную сущность давай с контекстом** (вид спорта/роль/«где шли Игры»);
  величины в одной единице; дистрактор правдоподобен в контексте; **без бинарных
  «X или Y?»**; **не называй в вопросе сущность, чья идентичность и есть вау**; **варианты без
  подсказки-дискриминатора** (века/размеры в скобках); **варианты по-русски, без англо-фраз**;
  **тема-фит** (ответ не вычисляется из категории; категория соответствует теме);
  **без живых рекордов/переходящих титулов как ответа** (протухают);
  **без абсурдных вариантов** (смешон в любом контексте → отсекается без знания);
  **дистракторы не вложены логически** («дороже золота» влечёт «дороже серебра»);
  **антипаттерн «экзамен»** («Почему X?» с 4 объяснениями-механизмами → переверни, X в ответ);
  **доли цифрами/%** в ответах, не прописью («Половину»/«Четверть»);
  **класс-когерентность** (вопрос называет класс/число ответа → все дистракторы того же класса);
  **без жаргона-термина в ответе** даже корректного («взрывные звуки» → «воздушные хлопки»);
  **ответы в именительном падеже** (вопрос навязывает косвенный → переформулировать вопрос);
  **дистрактор грамматически замыкает вопрос** (не обрубок «Французский физик», не соседняя тема);
  **без синонимов через «и» в одном варианте** («Болтовня и разговор» → одно слово);
  **живые рекорды/рейтинги — автодроп** даже в прош. времени (триггеры «до сих пор»/«самый кассовый»/«лучший в истории»).
  Точки — A.3 (сюрприз/вау/«экзамен»), A.5 (дистракторы/бинарность/дискриминатор/абсурд/вложение),
  A.8 (язык/англо-фразы/доли-цифрами), genPrompt + judgePrompt (`fill-questions`),
  polish RULES (классы 1г/1е/1ж/1з/2/3/4/7/9).
- Зависимые точки (обновлять вместе):
  - `scripts/lib/review-store.mjs` — `gatesOk` (жёсткие два — блок промоушна; подхватывает и merge-polish).
  - `scripts/merge-gen.mjs` — нарушение жёстких → понижение keep→revise.
  - `scripts/fill-questions.workflow.js` — genPrompt (правила) + judgePrompt (флаги cleanDistractors/naturalLanguage).
  - `scripts/polish.workflow.js` — класс 1(г) (диапазоны) + класс 9 (около-спам + хедж-в-1, «и»; дрейф-фикс: «и» не значимое слово).
  - `src/main.js` — `validateQuestions` (мягкое предупреждение).
  - `src/admin/admin.js` — предупреждения в карточке ревью.
  - `tests/content-rules.test.js` (юниты) + `tests/questions-answers.test.js` (жёсткий гейт по проду).
  - `docs/category-risks.md` (A.5 диапазоны/около, A.8 перечисление).
- **Принцип:** детерминированное (около/диапазоны/запятая) — КОДОМ, не на доверии LLM.

## Тире (— / – → дефис)

- **Каноничный код:** `src/content-rules.js` — `FORBIDDEN_DASH_RE`, `validateNoDashes`, `stripDashes`.
- Зависимые: merge-скрипты (автозамена), `polish` (правило 6), `docs/category-risks.md` (A.8).

## Правила показа карточки (порядок ответов, превью админки)

- **Каноничный код:** `src/card-rules.js` — `orderAnswers`, `imageCandidatePaths`.
- Зависимые: `src/main.js` (игра) и `src/admin/admin.js` (превью) импортируют ОДИН модуль —
  дрейфа нет by design. `docs/GDD_LITE.md` (§4).
- **Превью админки = игра ПО ОТОБРАЖЕНИЮ.** Что нельзя в превью (иначе расхождение с продом):
  - показывать картинку в карточке вопроса, пока картинки отключены в продовом `renderScreen`;
  - держать отличную от arcade UI ширину/типографику вопроса: карточка 624px, content 552px,
    вопрос 24px, answers grid 266×62 с gap 20×14;
  - перенос текста — ТОЛЬКО по словам: `hyphens: none`, `overflow-wrap: normal`. НИКОГДА
    `hyphens: auto`/`break-word` (рвёт слово «Папой» → «Па-пой», игра так не делает);
  - шрифт НЕ ужимать (см. лимит длины — не влезает, значит переформулировать);
  - верный ответ на карточке НЕ подсвечен (как в исходном состоянии прода).
  - **Гард:** `tests/admin-preview.test.js` ловит регрессы по исходникам (hyphens:auto,
    ужатие шрифта, отвязку от `card-rules`). Новое правило показа → добавь туда ассерт.

## Свод правил вопроса (факт-не-ярлык, sweet spot, анти-миф и т.д.)

- **Каноничный текст:** `docs/category-risks.md` (A.0–A.10 + риски по подкатегориям).
- Зависимые: genPrompt/judgePrompt (`fill-questions`) ссылаются на него; `polish` RULES (9 классов)
  дублирует операционно; `docs/quality-examples.md` — примеры ❌→✅, их теперь ЧИТАЮТ и генератор,
  и судья (не только анализатор). Анализатор ПРЕДЛАГАЕТ правки сюда (`docs/rubric-proposals/`),
  применяет человек; примеры он пополняет аддитивно (`merge-analysis.mjs`).
